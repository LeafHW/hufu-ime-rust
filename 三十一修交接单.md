# 三十一/三十二修交接单（2026-09-19 凌晨）

## 三十四修（2026-09-19 下午，本日第二项）
1. **段间键宽自校准**（默认生效）：单 CJK 字上屏置 `commit_cjk1` 旗，下一段
   首键锚点 − 上段 sticky 落点 = 真实全角字宽，半之 EMA 进 `caret_est_unit_w`
   （首次采纳、其后 0.5/0.5）。修 WPS（4.3→~7px）/QQ（~6→~11px）兜底
   0.41×行高的系统性低估——「虎娘更快」观感的根因（虎娘滑真实键宽，我们滑
   半个）。Typora 本就准（11.07）不变。同行 |dy|≤12 且 dx 4..=200 才采纳；
   一次性消费；焦点切换清旗。样本同时改善 est_step 步进精度。
2. **chase 实验通道**（旗标开关，第三种动效）：`C:\ProgramData\HuFu\diag\chase`
   文件存在=启用「窗口永远追赶光标」——目标=本帧锚位，起点=当前实位（首显
   =sticky=本段编码左端，真实位移零估算）；指数逼近 τ=35ms + 限速 2.5px/ms，
   子像素积分（chase_pos 浮点，live_pos 为取整镜像），dt 自适应免疫 tick 饥饿。
   与 pos_anim 互斥（臂发互清）；hide_now/focus_reset 硬清。即开即关无需重启
   宿主（每次 show 现查旗标）。candwin2 新字段：chase_target/chase_last/
   chase_pos；tsf 新函数 chase_on()、is_cjk_fullwidth()。

## 用户诉求
①切焦后首键响应慢+动效不对（三十修「完全没感觉」，用户要求推倒重想）；②延迟修好后，切焦首显动效仍有顿挫感（全宿主共有）。

## 根因（均实测复证，不依赖三十修结论）
1. **WPS 首段抑制门结构性必延迟**：焦点重置清 sticky → sticky_near 豁免永不可达；
   wps_stable 的 settle 计时器只在抑制帧武装 → 首帧 settle_start 恒 None → 即使
   wait_ms=0 也必吃一轮 35ms 补显钟；抑制过的首显再被 entered_late 剥入场滑。
   三十修减掉的等待被这一轮重试原样补回=「改了等于没改」。
   部署核验：四通道注册表+文件时间全对、WPS 进程晚于构建——确实是代码问题。
2. **入场滑时钟早计**：t0 臂于 show() 内，窗口实际可见还要过 Present(1) 垂直同步
   + DWM 合成；切焦后宿主 UI 线程最忙，延迟放大到 1-3 vsync → 窗口「出现在半路」
   再被 tick 拽着追=顿挫感（全宿主共有）。
3. 动画 tick 热路径两处未门控 format!（二十五修同款问题回潮）。

## 修复（均已部署四通道，未 commit）
- tsf.rs「三十一修·活锚即显」：伪首段（cand_shown_in_proc）+ 非表格 + TSF 活锚在
  → 首帧即显、入场照播。真首段 100ms 与表格段 200ms 维持。
- candwin2.rs「三十二修·首显时钟重锚」：SWP 显示后 t0 归零，滑动从实际可见帧播满
  全程。只动入场滑，逐键跟随节奏未碰。
- tick 两处 format! 门控。

## 实测数据（真实键入，trace 全链 + 2ms 窗轨迹采样器，2026-09-19 00:44-01:00）
- 按键→显示：WPS 中位 44ms/P90 48（n=107）；Typora 41/43（n=49）；记事本 8；
  虎魄 86/106（Qt 布局锁+自身抑制语义，用户未诉）。键→组段 0-1ms。
- 入场动画 146 例（5 宿主）**100% 按时完成**（dur+30ms 内），平均跨度 51-61ms，
  偶发单 tick 30ms 迟到（时间基准自动追平，无累积）。
- WPS 行程仅 4px（unit=4.32=半角键宽），Typora 11px——行程随字宽缩放是设计口径。
- 冷创建 0 次（预热有效）；抑制链只剩虎魄 104 次（该宿主锚链特性）+WPS 3 次。

## 残余已知问题（影响极小，未动）
- WPS 每段 ~200ms 后 1-3px 迟来微调（GetTextExt 懒布局收敛）。
- WPS 切焦后段 2 的 est 基线是垃圾值（est_x≈10 vs 真实 723）→ seg1 selection 活锚
  被「旧布局拦截」误拒 7 次，回退标准链结果仍正确，白付一次查询。
- Typora 2 次 GetTextExt 失败→60ms 跨帧重查自愈，正常。
- WPS 的 est 校准偏低（unit_w 4.32 vs 真实半角 ~7px）→ 首显滑行程偏小；修校准可
  让 WPS 行程到真实全角宽 ~14px（用户如嫌 WPS 滑动弱，这是下一旋钮）。

## 三十三修（2026-09-19 上午，三轮迭代后全部回退）
- 行程全角化 → 用户实测多数宿主「过头」→ 回退半角键宽基准。
- 入场快滑（7.5→4.0/100ms 上限）→ 用户随后要求时长也回退 → **恢复七修口径
  （7.5/160ms）**。三十三修净效果=零（动效参数全部回到七修状态）。
- **实际保留的只有三十一修（活锚即显=响应速度）与三十二修（时钟重锚+trace 门控）**。

## 工具
- 采样器：虎娘\分析\CandSampler.cs/.exe（2ms 候选窗轨迹+前台进程→CSV）
- 分析：虎娘\分析\entrance_analysis.py（逐入场动画按时率/断档）
- trace 旗标：C:\ProgramData\HuFu\trace\trace-on（用时创建，测完删除；
  注意 trace.log 文件 ACL——提权进程创建的文件普通进程写不进，删文件重建即可）
- 键盘注入在本环境仍全灭（SendInput/PostMessage 均被吃），真实键入只能靠人。

## 构建
cargo build（x64）/ --target i686-pc-windows-gnu（x86），冒烟全绿
（[18] 缩放档 1px 断言为负载敏感既有瞬态，与改动无关——新旧二进制对比复证）。
注意：本机 cargo 增量指纹偶发不可靠（mtime 欺骗），部署前删产物全量重链并用
哈希/大小核对。
