高亮选重滑动 + 渐隐渐现回退（motion159 暂留回归）

· 渐隐渐现回退：连打实测更闪（alpha 每次上屏都探底），恢复 motion159
  语义——上屏暂留 1s + 盒心收拢，连打零动画（窗口持续在场不闪）。
· 高亮选重滑动动效：数字/选重键上屏后高亮胶囊 0.24s 滑到选中项（uru+3
  高亮移到第 3 项）再照常暂留收场；↑↓ 移动高亮同款滑动（原瞬跳）。
  皮肤 layout.hl_ms 可调（0=关）。上屏零迟滞。
· 引擎选重闪帧：on_rank_key/select_candidate 上屏帧带回旧候选+高亮=
  选中项（raw 清空防幽灵组段；空格首选保持 is_idle 语义）；server 回包
  顶层 state 优先透传 outcome.state（此前重建把闪帧丢弃）。
· DLL 兜底：数字键上屏（锁+提前上屏路径如 uru+3=指了指）引擎 state 为
  空——update_ui 用 last_show 候选自建闪帧；停留钟停摆（宿主 WM_TIMER
  迟滞）超 2s 轮询兜底真隐藏，杜绝对窗滞留。
· 32 位 DLL 同批（x86 同源构建）。
