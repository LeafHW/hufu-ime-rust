入退场动画改左上角生长（盒贴左上，出入同款）

【用户口径】「入场出场一定是从左上角出来或回去，现在是整个高亮一
起」——v4 首项锚（f028a1d）仍是「盒中心趋near锚点」的对称缩放=高亮
居中一起出现，不是要的观感。
【修法】盒偏移 (bx,by) 恒 (0,0)=贴窗口左上角，删锚点偏移计算——
入场：外壳盒贴左上角从小块（72%）向右下拉开到全窗；退场：反向缩回
左上角小块（40%）再隐藏。盒尺寸仍由 size_anim 缓动（chrome_override）
驱动，窗口物理尺寸全程目标值（零位移不变量不变）。
【顺带】删 sizedraw 标签（unused 警告，改裸块）。
【验证】notepad 真机 u→空格上屏链路：show→暂留→退场完成→隐藏 正常。
