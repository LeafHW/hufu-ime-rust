入退场同锚首项高亮 + y 锁首帧微差仍钉（WPS 残余微抖收敛）

【锚点口径修正】用户澄清：要的不是「出入都在窗口中心」，而是「出入
都在首项高亮区（左上角）」。上一轮（89c0a53）把锚点统一到窗口中心
是改错方向——原始问题只是退场锚了「选中项」高亮。修正：
· hl_center 捕获条件 i==sel → i==0（横排首候选/竖排首行的胶囊中心）；
· 入退场共用此锚（读取点恢复 hl_center，缺省回退窗口中心）。
效果：入场从左上首项高亮处长出、退场收拢回同处——从哪来回哪去。
【y 锁首帧微差仍钉】D 轮（u+空格×30 单键上屏流）观测 y 微变回升：
fresh 首帧全放行让段首锚差 ±1/±2 直接透传。改：fresh 只放行 >3px
位移（跨焦点大跳照常自由），≤3px 微差钉住。
【实测收敛链】WPS 四轮 180 段复测：外部观测 y 微变 22 → 19 → 17 →
10 次（probe 补 DPI aware 声明排除虚拟化取整伪差后 10 次）——且全
部为孤立单帧 1-2px 步进（间隔 400ms+，非往复振荡，每 6.5s 一次，低
于感知阈值）；DLL 侧 show 目标 y 序列 959 帧 0 次微变、段级 4-26px
校正仅 1 次——引擎层 y 已稳定。「的的的」场景的可感抖动主体已灭。
