退场收拢改正圆（宽=高=min 边×50%）

【用户反馈】收拢时窗口是「椭圆」（等比缩小的长方形）不好看，要正圆；
比例改 50% 试试（原 40%）。
【修法】hold_fire 收拢目标从 (w×0.40, h×0.40) 等比长方形改为
side = min(w,h) × 0.50，tgt=(side, side) 正方形——收完是个正圆点再隐。
【验证】notepad u→空格上屏：退场起臂 (206,140)→(70, 70)（min 140×
0.5=70 正方 ✓）→200ms 收拢完成隐藏。
