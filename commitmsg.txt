三十六修：全仓审计整改打包 v1.6.3——丢字机制定位+断供兜底、学习保留、置顶失位、跟打器 Ctrl+Space、候选窗崩溃修复等

审计背景：2026-09-19 全仓审计（六路并行 ~47k 行全读+grep 验证），本修按报告优先级整改。基准：万句 A（部署态·护栏开）99.04% → 99.04%，错句集与基线逐句一致零回归；引擎 100 测试全绿；冒烟全过；x64+x86 双架构构建全过。

engine（hufu-engine/config/rerank）：
- select_first 断供兜底（丢字机制定位+安全网）：提前上屏锁定错误前缀后完整态候选池可耗尽（前缀过滤清空候选+码表对超 4 键剩码无精确词条），空格在空候选分支静默丢弃剩余码流——真死锁时丢弃 committed 约束、剩余 raw 重开一段解码取首选上屏（no-learn）。开关 sentence.empty_code_auto_commit 本轮接通（serde 默认 true，此前零消费）；bench 实证主流错型是「池活首选错」（cands=2/8），兜底对 bench 中性（错句集逐句一致）
- 会话学习保留：session_learned 登记→reload_user_data 重建后回放（adjust.removed 过滤/切方案清空）——置顶/删词一次清零整场调频的 BUG 根治
- 置顶失位：freq_boost take_while(pinned) 前导段保护 → 全列表稳定分区（#固 词被短语压前合并挤出前导段后失效的旧病根）
- Reranker::load 改 open_lazy（原全量 read_to_end 550MB）；TOP1500/Session::early_reset 死码删除；config 默认模型路径对齐 模型\ 布局；empty_code_auto_commit/min_retained_raw 注释改真实语义

hufu-tsf（tsf.rs/candwin2.rs/addword.rs/lib.rs）：
- candwin2 trunc_tail chars.len()-6 下溢 panic → saturating_sub（竖排+大字号崩溃级）
- Ctrl+Space TestKeyUp 通道（跟打器只投 TestKeyUp、此前 up 全拦=模式键永不可达）；modekey_down_seen 一次性标记挡普通宿主双发
- addword tid 登记移入窗口登记临界区（B1 残余竞态：双线程同过前置检查覆盖真窗口线程 tid）+ in_window_thread 单次 load
- TL fade_tick 补 chase/hl 步进（三十四修 chase 只接主线程一半：词框窗冻结）；WM_APP_HIDE 双分支清 chase、TL 补 Kill HIDE_LATER
- 断段空帧清 suppress_since/entered_late；rank_flash 尾部一次性收尾；query_caret 四早退恢复旧锚；POLL_SHARED 毒化自恢复
- chase_on 500ms 节流；恒真 SWP 主观测块删除（|tx-x|≡m_off）；三处 format!/diag 门控；NCHITTEST 不可达臂删除；hupo_* 五组死旗标+档案注释清理；DllGetClassObject 加载画像每进程一次

hufu-server/cli：
- pipe 服务端读超时：首帧 10s/帧头→帧体 2s（僵尸连接占满 255 实例防护；keep-alive 空闲不受限）；键流 trace 追加写（旧覆盖写只剩最后一键）+OnceLock 门
- /api/state model_present 补「模型\ 目录探测」回退（与 load_plan 同口径，消除「有模型显示未安装」）
- tbench 空数组 panic 守卫；cbench 拒绝纯码表整串喂码+TSV 健壮化+显式关 empty_code_auto_commit

settings-ui/发行链：
- weasel 配色导出 >>>0 无符号（round-trip 断裂修复）；「残码水位线」死控件删除；正文内部代号清除
- 发行脚本/与 platform install.ps1 同步 ←打包源（uninstall 32 位三处清理/下划线注册表清理/默认输入法覆盖段）；install.log 出库+.gitignore；5 个无 BOM ps1 补 BOM；canduielement.rs 孤儿文件删除

未动（审计已知留后续）：P9/P10 渲染缓存、P3/P5 补显与重查退避、GetSelection ulCount 取舍、学习持久化设计、README/architecture 文档更新。详见 三十六修交接单.md。
