清理机制代号字样（63 处→0）+ 纯码表纯频序 + 候选状态机补漏

【机制代号清理】全仓库（代码注释/文档/设置页/测试名）63 处机制代号
字样清零：统一改中性表述（满码顶屏/顶屏钳位/选重确认流等）；测试名
overmax_push 改 empty_clear_beyond_max；README 禁令行改写为不含代
号本身；拼音 overmax 亦清零。git grep 顶屏/overmax = 0。
【纯码表纯频序 2026-10-09 十五】用户口径：整句类算法只在整句方案
（「整句虎」+模型装载）生效；虎码字词等纯码表=多表同步合并后谁字频
词频高谁在前。「4 码内 1500 高频单字浮字」规则加 sentence_active()
域门（整句限定）+ 被压词收窄为 Sentence 来源。实测 is→
[经常(485) 络(397) 糸(258) 纟(257) 糹(0) ⺯(0)]（此前络 397 被浮到
经常 485 前）——虎码字词三表（tigress 主表 import tigress_ci +
tigress_simp_ci）合并后纯频序。
【测试对齐】4 个单元测试 + 2 个集成测试按新口径改预期（jd 原序
[就,到的,加]；freq1500 两测锁定纯码表纯频序）；3 个开关行为测试
显式开被测开关（默认值已改关：空码清屏/回车清屏/中英混输）+ config
roundtrip 测试补新默认断言。87 项测试全绿。
【候选状态机补漏 2026-10-09 十六】hold_fire 起臂退场（钟已杀）时
commit_hold 标记同步清空——否则退场被新键 show 打断后 poll stale
的「hold 在身不重臂」永真（钟不会再响）→ 候选永存。实测：暂留关 +
u 空格上屏 → hold起臂(0ms) → 10ms 退场起臂 → 200ms 退场完成→隐藏，
收场链全通。
