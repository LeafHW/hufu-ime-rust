二十九修：打包宿主首键滑入+开始菜单左上角幻影框根除+贴边钳制改纯内容

tsf.rs：
- 打包/owned 通道首显滑动（修1）：owned 宿主（新记事本/Store）此前
  首键无滑入（滑行注入只覆盖常规车道）——OWNED 分支同款注入：
  travel=max(raw_len,1)×unit（est 校准→行高×0.41→字号 em×0.5 三级
  链），fx=x-travel 起点，随首显帧带入 cw2 pos_anim；用户实锤记事本
  首键滑入生效
- 常规车道补 unit 兜底链（同三级链，est 灭时不再退化为零行程）
- 焦点切换首键卡顿部分改善（修2 部分）：退化锚点帧（0 宽/坐标原地
  跳变）过滤，FocusJank 实测 265→143ms；锚点源统一（三十修）再收口
- 开始菜单左上角幻影框根除（修4，现场取证定位）：五处历史 (12,12)
  兜底全部改真实锚点链（g.caret→gui 插入符→宿主窗矩形），锚缺失
  跳帧不画——①cloak 死亡帧（owned 分支）②cloak 死亡帧（常规分支，
  原「用户定稿固定左上角」旧方案）③cand2_dead 代画兜底 ④续帧车道
  ⑤cand2_dead 漂移自愈（字面硬编码 12,12，实锤源头：Deactivate/
  Activate 漂移时机触发、滞留到下次收窗——server 窗 rect=(-3,-3)
  与 srv-cand 日志 x=12 y=12 吻合）
- 诊断：trace 落盘路径 HUFU_TRACE 可指定字面路径（AppContainer 宿主
  %TEMP% 不可写的观测通道）

candwin2.rs：
- 屏幕边缘钳制改纯内容计算（修3）：钳制不再把阴影投影边距算进
  内容宽——贴边时候选文本不被提前收窄/换行

hufu-server（candwin.rs/pipe.rs）：
- 渲染线程锁中毒加固：九处 .lock().unwrap() 全改 unwrap_or_else(
  into_inner)——panic 后不再毒死 mutex 致代画窗永久停摆
- （本轮曾试 server 端首显滑动动画+SearchHost 全帧代画守卫/粘滞锚/
  熔断，用户实测回归后整体回退——开始菜单首键滑入未解决，维持
  v1.6.1「第二键起跟随」语义，三十修再攻）

部署教训（本修核心发现）：SearchHost（开始菜单宿主）加载
C:\Windows\SystemIME\HuFu\hufu_tsf.dll（HKLM 机器级），完全不读
HKCU 开发注册路径——对开始菜单的任何修复必须部署 SystemIME 验证，
否则改了也白改（本修前三轮全数无效的根因）。

冒烟：0x1FF 全绿；x64/x86 双架构构建通过。用户实锤：记事本首键
滑入正常、开始菜单候选跟随正常、左上角幻影框绝迹。
