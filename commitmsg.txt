预热排除打包宿主：开始菜单候选窗「跑右上角」修复

v1.6.0 实机回归：SearchHost（开始菜单）里 poll 闲拍预热建了普通 DComp
候选窗，OWNED 分支见 cand2=Some 直接复用——沙盒进程里 D3D/锚点异常，
候选窗落在屏幕右上角（1.5.9 无预热时 OWNED 分支首需 new_owned，位置
正常）。修复：预热条件加 !host_is_packaged()——打包宿主保持 1.5.9
行为，普通桌面宿主预热照旧（首键 17ms 提速保留）。

随行：码表\虎码单字 恢复 1.5.9 原样（tiger.dict.yaml 主码表——真机
镜像时误带了虎码单简码表）。