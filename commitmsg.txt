默认值修正：空码清屏/回车清屏/中英混输默认关 + 每页 4 + 尺寸过渡 100ms

【用户拍板】①「空码自动清屏（满码+1 才清，保末键）」「回车清屏」
「中英混输（保留大小写）」默认关（原默认开，新用户易困惑：编码
突然没了/大小写字母混进候选）；②每页候选数默认 4（原 5）；③候选
变宽变窄动效 60→100ms 试试。
【改】hufu-config InputSection：auto_clear_empty/enter_clear/
mixed_input 默认 true→false；CandidatesSection.page_size 5→4；
candwin2 size_ms 默认 60→100（与入场共用 size_anim，同过渡时长）。
【本机】config.json 已显式写死旧值——API 热翻正（auto_clear_empty=
false/enter_clear=false/mixed_input=false/page_size=4），即时生效。
【验证】notepad u：候选窗 h=103（原 5 项时 140——每页 4 生效，窗口
矮了）；代码默认对全新安装/恢复默认值生效。
