无模型=纯码表总闸：口径测试锁死（十九修）

【用户口径】模型存在与否是唯一算法开关：安装根目录无模型文件夹/
相关内容时，任何方案（含名字带「整句」的整句虎）都是纯码表——
码表原样候选、顶屏/空码/选重/翻页纯码表语义，没有任何算法介入；
检测到模型时整句算法才介入。
【现状审计（全部已就位）】引擎所有整句类算法路径均以「解码器在
身」为前提，而 server 的 sentence_load_plan 只有 ngram 路径真实
存在才装载解码器：
  · 整句解码接管/浮字/短语压前 → sentence_active()（内含
    sentence.is_none()→false）
  · 提前上屏 try_early_commit → 无解码器直接 return
  · 语料挂起 supp_hold_blocks → 仅被提前上屏调用
  · 语料词图注入 sync_sentence_user_words → if let Some(dec)
  · 神经重排 apply_rerank → rerank 缓存空（无解码器不产生）早退
【总闸测试】no_model_pure_dict_total_gate：虎整句原生格式码表
（一行多候选空格分隔）+无 decoder，断言六项行为快照——①无解码
器且整句不激活（哪怕方案名带整句+配置开）②bu 候选=码表原序
[建,好的] ③bu; 次选=好的 ④空格首选=建 ⑤数字 2=到的 ⑥满 4 码
第 5 键顶屏码表首选+新串起点。防任何算法路径在 sentence.is_none()
时漏出。
【用户机器实况】D:\HUFU\...\模型\ 下实际存在 mohu-sentence-
ngram-v5.bin + sentence-qwen-q8.gguf 两个模型文件（用户以为无模
型）——整句算法因此在机启用；bu 短码两态同为码表原序（整句仅
接管超长/带锁流），「好的」一选系学习调频（十八修已修已清）。
真机复核：空格/数字选重学习照旧（本轮 +2 行正确），; 零学习。
移走模型文件夹+重启 server 即得纯码表态，放回即恢复。
