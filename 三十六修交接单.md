# 三十六修交接单（2026-09-19 下午）

## 背景
全仓审计（2026-09-19，六路并行 ~47k 行全读+grep 验证）后按报告优先级整改并打包 v1.6.3。审计报告见当日会话；旧审计 docs/审计与整改-2026-09-13.md 的 P 项核对结论已并入本版注释。

## 本版改动（按文件）
- **hufu-engine/src/lib.rs**
  - `select_first` 断供兜底（丢字根治）：空候选+committed_text 非空+raw 非空 → 丢弃 committed 约束、剩余 raw 重开一段解码取首选上屏（refresh_candidates 复用，no-learn）。开关 `sentence.empty_code_auto_commit`（本轮接通，serde 默认 true）。
  - `Engine.session_learned` 会话学习登记/回放：learn 时计数，reload_user_data 重建后回放（`adjust.removed` 过滤、切方案清空）——修「置顶/删词一次清零整场调频」。
  - freq_boost_domain 置顶保护：`take_while(pinned)` 前导段 → 全列表稳定分区（pinned 恒前、保相对序）。
- **hufu-config**：empty_code_auto_commit/min_retained_raw 注释改真实语义；Default ngram_path/model_path 改 `模型/`（对齐发行布局，消除「有模型显示未安装」的口径错位）。
- **hufu-server**：pipe serve_conn 首帧 10s/帧头→帧体 2s 读超时（wait_avail/read_exact_dl；keep-alive 空闲不受限）；key 流 trace 追加写+OnceLock 门；/api/state model_present 补「模型\ 目录探测」回退（对齐 load_plan）。
- **hufu-rerank**：Reranker::load 改 `GgufFile::open_lazy`（原 open 全量 read_to_end 550MB）。
- **hufu-tsf/candwin2.rs**
  - trunc_tail `chars.len()-6` 下溢 panic → saturating_sub（崩溃级）。
  - TL fade_tick 补 chase/高亮滑动步进（chase 通道此前只接主线程）；WM_APP_HIDE 双分支清 chase 三元组、TL 分支补 Kill HIDE_LATER。
  - chase_on() 500ms 节流；四十一修恒真观测块删除（|tx-x|≡m_off）；三十九修/pin 诊断 format! 门控；NCHITTEST 不可达臂删除（头部 HTCLIENT 强制使其永不可达，B4 项关闭）。
- **hufu-tsf/tsf.rs**
  - Ctrl+Space TestKeyUp 通道：mode_key 的 up 放行（跟打器只有 TestKeyUp）；`modekey_down_seen` 一次性标记挡普通宿主双发（按住不限时长不翻回）。
  - 断段空帧清 suppress_since/entered_late（update_ui 空帧分支+poll 空帧分支）；rank_flash 尾部一次性收尾（提前上屏帧不滞留）；query_caret 四个早退恢复 prev_caret/prev_line_end；POLL_SHARED 四处 unwrap → into_inner。
  - 死旗标删除：hupo_last_jump/reseg/reseg_armed/seg_raw0/long_mode/prev_raw（五十六~五十八修机制从未实现，注释归档）；trace() 死 static ON 删。
- **hufu-tsf/lib.rs**：DllGetClassObject 加载画像 OnceLock 每进程一次。
- **hufu-tsf/addword.rs**：tid 登记移入窗口登记临界区（B1 残余竞态收口，输家不碰 tid）；in_window_thread 单次 load。
- **hufu-cli**：tbench 空数组 panic 守卫（纯码表 sent_events/空语料 key_us）；cbench 拒绝纯码表（exit 2）、关 empty_code_auto_commit、TSV 畸形行计数/CR 剥除/表头跳过。
- **settings-ui/index.html**：weasel 导出 `>>>0` 无符号（round-trip 修复）；「残码水位线」死控件删除（JS 两处引用同步）；正文内部代号清除；「空码自动上屏」改「断供兜底上屏」并更新说明。
- **发行链**：发行脚本/ 与 platform/windows/install/install.ps1 同步 ← 打包源（install 多了下划线注册表清理+默认输入法覆盖两段、uninstall 32 位三处清理、设置.bat 安装根布局）；platform/windows/install/install.log 出库+.gitignore；5 个无 BOM ps1 补 BOM（check-dll/kill-server/walk-gguf/pipe-battery/deploy-31fix）。
- **删除文件**：canduielement.rs（mod 早已删，孤儿 130 行）。

## 实测
- 引擎 100 测试全绿；冒烟全过（[18] 缩放档本轮未触瞬态；[19] 拉伸圆角 mask=00 与基线同）。
- 万句 A（seed42，8 分片，护栏开）：99.04% → 见更新日志 v1.6.3 行（跑完回填）。
- 双架构构建：x64 default/x86_64-gnu/i686-gnu 全过，无新增告警。

## 验证注意（给下一轮）
- **部署双通道**：测试前宿主必须重启（DLL 按进程加载一次）；x86 注册表指 i686 产物。
- 丢字兜底行为变化：断供句尾空格现在会「猜着上屏」（换字≠丢字），bench 准率应升、残码降；若观感异常可设置页关「断供兜底上屏」对照。
- Ctrl+Space 修复只影响跟打器类宿主；普通宿主逻辑等价（down 直发+up 吞）。
- chase 实验通道语义未变（TL 词框窗现在会真的追了——此前冻结）。

## 未动（审计已知，留给后续修）
- P9 画刷缓存/P10 测量缓存/P11 size+hl 同 tick 双 show（渲染重构，单独一轮）。
- P3 段首补显重跑 SetPreedit、P5 重查无失败退避。
- GetSelection ulCount=u32::MAX 取舍（注释在案；动它要过宿主兼容矩阵）。
- sound.rs 句柄池无记账、ipc BG 超时与注释不符、EXCEL6_HIT 全局原子。
- 学习持久化设计（调频仅内存+user-adjust.log 只写不读——重启即失是现状口径）。
- README/architecture.md 文档过时；tray.rs 死托盘链；rerank gguf tensor_f32 越界防御（该函数本身死码）。

## 打包
- 打包源 更新日志.txt 顶部加 v1.6.3 条目；`E:\DSH-KF\hufu-发行\pack.ps1 -Version 1.6.3 -NoModel`（无 UAC 模式需先停 hufu-server；ctfmon 守护会自动拉回）。
- 本地 commit，**不 push、不部署**（用户验证后自行决定）。
