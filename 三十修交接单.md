# 三十修交接单（2026-09-18 深夜）

## 用户诉求
切焦点后的**首键**：出候选慢 + 入场滑动卡/拖尾。主战场 WPS 文字，其他程序也有。

## 今晚实锤的三个根因（均已处理）
1. **WPS 是 32 位进程**，走 HKCU WOW6432Node 注册 → 指向 `D:\HUFU\...v1.6.2...\hufu_tsf32.dll`（旧发行包）。
   我的历次部署只更新 x64 → **WPS 整晚跑 v1.6.2 旧代码，"一点没变"字面成立**。
   已修：HKCU 32位 → `E:\DSH-KF\hufu\platform\windows\target\i686-pc-windows-gnu\release\hufu_tsf.dll`；
   并补 `C:\Windows\SysWOW64\SystemIME\HuFu\hufu_tsf32.dll`（此前不存在）。
   **教训：部署必须双通道（x64+x86）**。
2. **DLL 按进程加载一次**：用户常驻程序（DSH 界面/QQ/哔哩哔哩/explorer）不重启就永远是旧库。
   测试/让用户体验新代码前，目标程序必须重启。v1.6.2 安装包本身双通道没问题。
3. 探针合成键盘注入被环境隔离吃掉（鼠标可注入、键盘不可；进程树内/外、SendInput/PostMessage/计划任务全试过均死）。
   **本机自动化打字测不了，只能真人键**。trace 通道（旗标文件 C:\ProgramData\HuFu\trace\trace-on → 同目录 trace.log）已验证可用，但今晚已按用户要求删除关闭。

## 代码改动（tsf.rs，已构建+已部署 SystemIME 双通道，未发版、未 commit）
- 三十修 part1：`cand_shown_in_proc`（切焦伪首段 WPS 100ms 等待→0，真首段仍 100ms）；`suppress_since` 500ms 熔断与 `g.caret.is_some()` 解耦。
- 三十修 part2：`no_anchor` 判链尾锚（有效系统插入符在 → 立即显示，不再 35-70ms 抑制重查）；`entered_late`（抑制等待过的首显不播入场滑，直接落位）。
- trace 开关统一：trace() 原有独立 env 闸 → 统一走 trace_on()（旗标文件/env 双通道）。
- 自带宿主探针回归全绿：首键 10-14ms + 9-10px 滑，与基线一致。
- 用户真实打字验证过一次（22:47 后新 x86 库在 WPS 里全链路 dispatch→cands→show→SWP 走通），但动效流畅度未完成量化。

## 残余已知问题
- tsf.rs L4305 `caret.or_else(gui_caret_fallback)` 未过滤退化点矩形（修2残留洞）。
- FADE_TICK_MS 4ms 在高刷屏；选中闪 150ms→瞬时未做；「暗季 1000」行未处理。
- 焦点重置是否清 `suppress_since`/`entered_late`：已加（entered_late/suppress_since 在 focus reset 与 show 尾均清）。

## 关键路径
- 源码：E:\DSH-KF\hufu\platform\windows\hufu-tsf\src\tsf.rs（~5900 行）
- 构建：`cargo build --release -p hufu-tsf`（x86 加 `--target i686-pc-windows-gnu`）；冒烟 `HUFU_TSF_SMOKE=1 .\target\release\hufu-tsf-smoke.exe`（首跑 101 瞬态，重跑过）
- 打包：E:\DSH-KF\hufu-发行\pack.ps1 -Version 1.6.3 -NoModel（唯一入口；打包源=更新日志.txt）
- 探针（废弃的注入路线但窗口采样逻辑可复用）：E:\DSH-KF\虎娘\分析\FocusJankExt.cs（含 2ms 候选窗轨迹采样+CSV）
