# 四十三修交接单：WPS 表格提速 + Excel 逃逸根杀 + 飘移根杀（实机探针取证版）

> 日期：2026-09-22 凌晨。三十七修实装后用户实测驳回（「修了跟没修一样，
> WPS 还是慢、还多了候选飘移，Office 表格每个单元首键位置都不对」）。
> 本修全部结论来自本机实机取证（探针 + trace），不再假设。

## 一、取证工具（本机新基建）

- `E:\DSH-KF\.etprobe.ps1`：候选定位探针。SendInput 真输入路径（点击+按键），
  5ms 轮询 `HuFuCandWin2` 可见性（首显时延+矩形），600ms 位移轨迹（飘移检测），
  GUITHREADINFO 插入符快照。支持多进程名（`'wps,et'`——WPS prometheus 候选窗在
  et.exe 进程里）、`-TitleMatch` 精确选窗、最小化自动恢复、前台校验失败不发键。
  **教训：脚本必须 ASCII 注释或 UTF-8 BOM**——无 BOM 中文注释的「）」尾字节在
  GBK 解析下吞掉 CRLF，代码行并入注释静默失效。
- trace 旗标：`C:\ProgramData\HuFu\trace\trace-on`（500ms 热生效，DCOM 宿主可用）。
  诊断后已撤（生产态）。

## 二、根因（trace 实锤，与三十七修的假设对照）

### WPS 表格（x86，键处理在 et.exe，可见框在 wps.exe——prometheus 分体架构）
1. **慢的真因**：单元格编辑器（Qt 自绘光标）没有系统插入符 → 三十七修的
   live_trusted/cell_trusted 恒假 → cell 分支唯一出口=四十修 200ms 死线。
   实测 +62ms 起 4 帧锚点已正确稳定仍被压到 +248ms。
   （三十七修「活插入符即显」的快路在 WPS 表格**从未点火过**。）
2. **飘移的真因**：用户实际节奏下收场钟残留窗跨格仍可见 → candwin2 逐帧
   平移臂（七十四修全平移）把换格 648px 也当连续位移长滑。
3. 附带发现：二级插入符扫描只扫前台进程（wps.exe）线程，漏自家进程
   （et.exe）线程——已并入修复（见 §三-1）。

### Office Excel（x64，单进程）
1. **「每个单元首键位置不对」的真相**：首显其实又快又准（+4~102ms，
   锚=所点格窄框）——**~195ms 后**焦点抖动路径（CommitFocusRevoke 保组段）
   把公式栏编辑条整框 `(750,531,2334,553)`（1584px 宽、常数矩形）当锚直接
   喂进 show，候选追其右缘 x=2334 逃逸。该宽锚**不经 update_ui 锚链**，
   三十七修装在 update_ui 的门拦不住（复测确认只拦到 1 次，宽锚照样进）。
2. 真 Excel 的格编辑框类名就是 `EXCEL6`（精确 6 字符，实测子窗 dump），
   ed6 锚网天然覆盖 Excel；`EXCEL<` 是另一回事（网格容器，7 字符，不相干）。

### 注册表复查（三十七修追记的修正并未生效）
- 2026-09-22 02:43 发现 HKCU 64 位 InprocServer32 **仍指向 target-fix 构建目录**
  （上次会话的回写没有落盘）。已重写并四视图回读核验（HKCU 64/32 + HKLM 64/32
  全部= SystemIME 双通道）。教训：部署脚本必须 reg.exe 回读，不能只看写入成功。

## 三、修复内容（tsf.rs / candwin2.rs）

1. **自家进程线程纳入插入符扫描**（gui_caret_fallback 二级）：
   `owner_pid == fg_pid || owner_pid == 自家 pid`。WPS 分体架构下编辑线程在
   自家进程，原先永远扫不到。
2. **框内锚即信**（anchor_in_cell）：GetTextExt 查询锚落在当前 EXCEL6 框
   ±40/60 容差内＝报的就是本格（换格旧值滞留必在框外——三十八修几何判据
   反用为信任判据；ed6 滞后拍时锚在框外→不信，保守不出错）。
   `cell_trusted = cell_seg && (live_trusted || anchor_in_cell)`。
3. **咽喉宽度门**（candwin2.show 入口，所有车道必经）：表格宿主
   （excel/wps 系）+ 锚宽 >400px → 降级无锚帧，窗口钉上一好位。
   专杀公式栏整框类垃圾锚（实测 9 次命中全部吸收）。update_ui 层的门保留
   （少一轮 poll 空转）。
4. **跨格大跳瞬落**（candwin2 逐帧臂）：位移 >500px 瞬落不滑。
   七十四修的上屏大串档（≤400px 随文平移）不受影响；实测换格 648px、
   逃逸回位 1168px 全部瞬落。三十二/三十八/七十四修教义链注释完整保留。
5. host_is_wps()/host_is_excel() 改 pub（candwin2 咽喉门用）。

## 四、实测（同一探针、同机、修复前后对照）

| 场景 | 修复前 | 修复后 |
|---|---|---|
| WPS 换格首显 | 270/301/270ms | **93/77ms**（进程首段 208ms 一次性） |
| WPS 同格连打 | 200ms/段（打快不显示） | **~45ms/段**（用户实打 trace：+43~46ms/键） |
| WPS 飘移 | 648px 长滑 | **零位移**（moves 单点稳态） |
| Excel 首显 | 17~102ms（快） | 15/32/18ms（不变） |
| Excel 逃逸 | 三格全部飞到 (2305,528) | **零逃逸**（咽喉门 9 命中；用户实打全程健康） |

部署：SystemIME 双通道（回滚点 `.old-223549` / `.old-024332`），
注册表四视图已核验，双架构构建+TSF 冒烟全绿。宿主需重启才加载新 DLL
（探针实测的进程均已重启验证）。

## 五、遗留与注意

- 微信（Weixin.exe）定位偏 1 字符＝微信侧缺陷（InputTip#163，三十七修
  调研定论），未动代码；通用对策（活锚/咽喉门）已覆盖。
- 首段 208ms（进程首次显示前的旧布局防御）保留：一次性、位置正确，
  换来病理帧防错。若用户仍嫌慢，下一刀是真首段 est 播种，别动死线。
- `engine/crates/hufu-sentence/src/model.rs` 有非本修的未提交改动（前会话
  残留），未并入本提交，归属待查。
- target-fix 下有 `.held-*` 改名旧 DLL（进程仍映射着，删不掉），无害。
