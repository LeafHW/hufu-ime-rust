# 虎符全项目审计与整改方案（2026-09-13）

## 审计范围与方法

| 审计对象 | 方法 | 状态 |
|---|---|---|
| tsf.rs（4414 行） | 全文通读 + 48 万行 trace 实测交叉验证 | ✅ |
| candwin2.rs（3806 行） | 全文通读 + 全项目 grep 调用关系验证 | ✅ |
| 其余 8 源文件（addword/langbar/lib/ipc/sound/com/canduielement/dll_entry） | 逐文件精读 | ✅ |
| v1.5.2..HEAD 63 提交 | 逐提交 diff 审读 | 进行中 |
| 外部参考 | 微软 IME 规范 / weasel PR#269 / SampleIME 源码 | ✅ |
| 部署链 | 注册表四路实查 + 文档对照 | ✅ |

## 项目全貌（理清结论）

```
engine/（10 crates ~17k 行，纯逻辑，285 测试）→ 问题不在 engine
  └ hufu-server：引擎宿主+管道+HTTP 设置页（engine/crates/hufu-server）
platform/windows/hufu-tsf/（10 文件 12k 行）→ 33 个补丁全在这，BUG 巢穴
  ├ tsf.rs     4414 行：按键 dispatch/组段/锚点/焦点/轮询
  ├ candwin2.rs 3806 行：候选窗渲染/动效（show() 单函数 2160 行）
  ├ addword.rs 1189 行：/jc /jq 词框（独立线程+TL_CAND2）
  ├ langbar.rs 1011 行：中英指示牌+右键菜单（v6 延迟写+thread_local sink）
  └ 其余：ipc(管道) sound(音效) com(注册) lib(导出) canduielement(死)
数据流：按键→dll→命名管道→server/engine→回包→组段+候选窗
部署：四路注册表（HKCU→D:\HUFU 最新✓；HKLM SystemIME 双路=09-12 落后5修✗）
```

## 用户三大主诉与实现现状

| 主诉 | 现状 | 结论 |
|---|---|---|
| 开始菜单/UWP 候选 | v1.5.2 已有 server 代画（cand_ui_active+ipc cand 帧）+ 1.5.3 周期加 owned-ulw 窗 | 核心已在，owned 窗是补丁层 |
| 中英指示符 | langbar v6：全局原子+每线程 defer 窗+thread_local 单槽 sink | 五轮修复后稳定，残留 HICON 故意泄漏 |
| 右键失效 | 缓存取数+自建 TOOLWINDOW owner+WM_NULL+全异步 | 已修，残留 OnMenuSelect 一处同步管道 |

## 审计发现汇总

### 甲、性能（跟打器 61ms/键，正常应 <20ms）
| # | 发现 | 位置 | 修法 |
|---|---|---|---|
| P1 | .NET 自绘 caret 宿主 gui_caret_fallback 结构性 None → 跌穿标准链 GetTextExt(30-60ms) | tsf.rs L2244 无 else 守卫 | hupo+fallback None → 走 est 步进（0ms），仅 est 无基线时首键一次 GetTextExt |
| P2 | 自动上屏必武装 60ms CARET_TIMER 全套重查（第二次会话） | L2619/L3918 | hupo 宿主跳过 |
| P3 | 段首键 35ms 补显重跑整个 SetPreedit 而非只查锚 | L2582/L2785 | 改走 Op::QueryAnchor |
| P4 | GetTextExt 固定双查、段首最多 4 次 | L2306/L2219 | 慢宿主单查取首次成功 |
| P5 | 60ms 重查无失败退避（持续失败宿主=循环全量会话） | L2375/L2384 | 连续 N 次失败退避 |
| P6 | 每帧读 stage.txt/pin.txt（毛玻璃已退役的取证钩子） | candwin2 L2282/L3192 | 删 |
| P7 | 每帧 GetProcAddress 解析 dwmapi | candwin2 L3405 | OnceLock |
| P8 | 每帧两条无条件 diag_note 写日志 | candwin2 L3436 | 删/门控 |
| P9 | 10 个画刷每帧重建（字体有缓存画刷没有） | candwin2 L2716 | 缓存 |
| P10 | 文本测量无缓存每帧 CreateTextLayout ×N | candwin2 L1801 | 测量缓存 |
| P11 | fade+size 同 tick 双 show（×67fps 放大） | candwin2 L3868/L3894 | 单 show |

### 乙、正确性（真 BUG）
| # | 发现 | 位置 | 危害 |
|---|---|---|---|
| B1 | ADDWORD_TID 竞态：窗口存活连按 /jc，B 线程覆盖 tid 后复用返回，tid 悬挂 | addword L245 vs L336 | 词框打不了中文；每次泄漏一套 TIP 激活 |
| B2 | WM_APP_HIDE 不分线程：TL 词框窗隐藏消息 take 主窗实例清其动效状态 | candwin2 L342-382 | 主窗动效被跨线程重置（残留窗病根未修的腿） |
| B3 | 滚轮路径无 TL 分流（跨线程 show=残留窗同型） | candwin2 L236-287 | 残留窗 |
| B4 | NCHITTEST 经 g.cand2 读 content_size（TL 窗用主窗数据） | candwin2 L315 | 穿透误判 |
| B5 | OnMenuSelect 同步 ipc::call（违背自设回调零管道原则） | langbar L854 | explorer 卡顿风险 |
| B6 | sound::invalidate 无调用（换 wav 后缓存永不失效） | sound L364 | 音效更新无效 |

### 丙、死代码（~1000+ 行，全部 grep 验证零调用/零读取）
| 块 | 位置 | 行数 |
|---|---|---|
| canduielement.rs 整模块 + tsf.rs cand_ui*/host_draws 死分支 | 全文件 | ~130+ |
| shadowwin 整链（SHADOW_HWND 恒 None，7 调用点空转） | candwin2 L4051-4422 | ~370 |
| capture_screen_rgba | candwin2 L755-806 | 52 |
| hufu_test_fancha | lib.rs L374-515 | ~140 |
| caret_force（4 置位 0 读取）/compose_at/caret_at/host_follow_caret 恒 true | tsf.rs 散布 | ~80 |
| last_raw_len 字段（只写不读） | candwin2 | ~6 |
| langbar SINK_PTR/push_compartments/refresh_sound_snapshot/Drop 空壳 | langbar | ~40 |
| ipc::ping / com::register_profile / addword current_hwnd/HILITE_BRUSH/cid | 各文件 | ~60 |
| tsf.rs pinned_now 死块 + 调试 trace 残留 | tsf.rs L2939/L2972 | ~8 |
| 渐隐退场路径（fade=Some((false,..)) 不可达） | candwin2 四段 | ~50 |

### 丁、结构问题（「修一个复发一个」的机制）
| 发现 | 说明 |
|---|---|
| show() 2160 行单函数 | 测量/布局/动画/渲染/呈现/定位/诊断七合一 |
| 动效状态 5-7 写点散布 7 入口 3 上下文 | pos_anim 7 写点、size_anim 6、fade 5；live_size 两种口径；sticky_pos 三重语义 |
| 焦点清理三裁判叠层 | 鼠标守卫吞 epoch / same_doc 对 Excel 无效 / 真裁判排第三 |
| wps_caret_prev 收敛算法从未实现 | 只用了 .is_some()，注释描述的三代政策叠层误导 |
| est 一修/二修模型矛盾 | 单行钳制按已推翻的模型活在 3 调用点；注释三代未更新 |

### 戊、外部参考结论（weasel/SampleIME/微软规范）
| 准则 | 虎符现状 |
|---|---|
| 无跨会话位置记忆，显示前必现查（GetSelection+GetTextExt） | 三十一修方向一致 |
| 失焦隐藏候选窗+清理 | 三十三修补齐 |
| 首显瞬跳 | 三十二修对齐 |
| 候选窗 owned-window | UWP 已用（owned-ulw），普通进程独立窗（可接受） |
| NOLAYOUT/失败宁可不更新也别落错位 | est 兜底已在，需 P1 修法收口 |

## 整改执行序（等逐提交审计到后最终定稿）

1. **第一批·性能四刀**（P1-P4）→ 跟打器 <20ms 验证
2. **第二批·渲染瘦身**（P6-P11）
3. **第三批·真 BUG**（B1-B6）
4. **第四批·死代码大清理**（丙全表，~1000 行）
5. **第五批·部署链修复**（HKLM 同步 + install.ps1 三份对齐 + 安装卸载验证）
6. **全量回归**（脚本家族 + 用户实测三大主诉 + 跟打器/WPS/Excel 兼容矩阵）
