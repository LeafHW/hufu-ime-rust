# 三十七修交接单（2026-10-21 晚）

## 背景（用户三连报）
1. **Office 表格（excel.exe）**：不同单元打字，首编码候选不在附近（定位错），但很流畅。
2. **WPS 表格（et.exe）**：不同单元打字首编码候选框出来极慢；同单元内打快了候选直接不显示（候选没出来就上屏）。
3. **电脑端微信**：有用户反应候选定位不太准——要求先上网查资料确认问题。

## 根因定位

### A（共因·二十九修回归）活插入符在首显锚链结构性失效
二十九修（2026-09-18）为首显锚加了「点矩形过滤」`filter(bottom-top>2)`，意图拦掉
GUITHREADINFO 的退化点值；但 `gui_caret_fallback` 内部把**所有**返回矩形归一成
0 高点（top==bottom==caret 底线）→ 过滤恒假 → 活插入符（WPS/Qt 真光标线）从那
时起在首显锚链整链失效，一切回退 GetTextExt/est。Excel「错但流畅」、WPS 表格
少一条最快真值源，皆源于此。

### B（Excel）无 settle 门 = 换格旧锚直显
settle/抑制链只盖 `host_is_wps()`；Excel 换格首键帧编辑器未建（键事件先于窗口
创建）→ 无活插入符，GetTextExt 给整编辑框矩形（trace 实测宽 1584px、left=框左
缘≠点击处）或上一格旧值，est 也还是旧格基线——任何一个都能**立即直显**（用户
「不在附近但很流畅」）。另外 query_caret 的 gui 兜底 est 连续性过滤在换格时
（dx 可达数百 px）会把真插入符当烂锚否决。

### C（WPS 表格）四十修固定 200ms「宁慢勿错」在表格会话=每段必等
`cell_seg = ed6_prev.is_some()`（会话见过 EXCEL6 编辑框）后**每个新段首显**都走
固定 200ms 等待（布局收敛期）：同格连打空格 90ms 先到 → 候选从未显示；换格更
是每格 200ms（「很慢很慢」）。位置对是因为等到的是布局收敛后的真值——错位防线
的代价是速度。

### D（微信·调研结论，未加专治代码）
社区实证：[InputTip #163](https://github.com/abgox/InputTip/issues/163)（微信
4.0.3.43 / Win11 24H2 / 小狼毫 0.16.3 环境）：**微信 4.0（Weixin.exe）在
Alt+Tab 切回窗口或重开窗口后，上报的光标位置偏左约 1 字符，手动点击一次输入框
才恢复**。这是微信侧的光标上报缺陷（所有依赖光标位置的工具同样中招；InputTip
的对策是换光标获取模式+手工偏移，没有根治方案）。输入法侧可做的最优先对策=
不要轻信宿主上报值，首显锚优先独立真值（系统插入符）——本修 A/B 两项恰好把
这条链修通（首显活插入符优先 + 段首 GetTextExt 双失败时活插入符直采）。若用户
仍报偏移，下一步让反馈者提供 `%TEMP%\hufu-tsf-trace.log`（qc: raw=… 序列）定位
微信报的是 GetTextExt 偏移还是 selection 偏移，再决定是否加微信专属纠偏。

## 本版改动（全在 platform/windows/hufu-tsf/src/tsf.rs）

- **gui_caret_fallback·插入符保高**：caret_valid 返回矩形改如实保留高度
  （top=caret 顶、bottom=caret 底线，均 ClientToScreen；left/right/bottom 与旧值
  逐字段一致，仅 top 由「=bottom」改为真顶）。真插入符（行高≥8px）通过首显的
  点矩形过滤被优先采纳；退化点值（高 0）依旧被滤——二十九修「切窗首键动效卡」
  防线语义不变。首显滑动的 anchor_h 档、框外过滤同步拿到真实行高。
- **host_is_excel()（新）**：exe 含 "excel" 即命中（OnceLock 缓存）。
- **settle 门扩容**：`wps_settle = (host_is_wps() || host_is_excel()) && …`——
  Excel 纳入首显等待链治理。
- **live_caret 单查复用**：update_ui 首显锚与信任判定共用一次
  GUITHREADINFO（原链两处各查一次）；first_show 锚 = `live_caret.or(g.caret)`
  （原「filter 恒假」死链复活）。
- **live_trusted / cell_trusted·表格活锚即显**：活插入符存在 且（无 EXCEL6 框
  或插入符落在框 ±40/60px 内）→ 视为真值就绪：
  - 同格连打（编辑器常驻）→ 帧 1 即显（0ms，原 200ms）；
  - 换格首键（编辑器 ~35ms 建立）→ 补显帧即显（~35ms，原 200ms）；
  - 无活插入符的病理帧维持原 200ms 等待（宁慢勿错保留），120/500ms 熔断不变；
  - 首段/伪首段分支的 wps_stable 同样被 live_trusted 短路（活插入符不必陪
    GetTextExt 布局走完 100/200ms 等待线）。
- **excel_suspect·Excel 首键锚疑**：Excel 首显帧若无活插入符，fresh_anchor/
  sticky_near 两个「即显豁免」一律失效，强制走 35ms 补显等待（编辑器建立+
  插入符到位即显，~35ms 位置直接正确）；有活插入符时豁免照常（流畅不变）。
- **query_caret·段首活锚直采**：GetTextExt 双失败后的系统插入符兜底，seg=1
  （段首）无条件采（est 基线还是上一格/上一文档旧值，换格 dx 可达数百 px 会把
  真插入符当烂锚否决——Excel/WPS 换格首键错位的帮凶）；段内（≥2 键）维持原
  连续性过滤不变。

## 并入提交（前会话遗留，非本修改动）
「特效退役 2026-09-22」轮次的未提交残留（commitfx/落印字号链路删除；engine
config 字段/pipe 注入/settings 控件/candwin2 color_f 与 ensure_thread 清理；
Cargo.toml Win32_UI_Accessibility 特性）。该轮产物已由前会话部署（根目录
.deploy-v7fx-* 日志），本次一并入库保证源码=部署物。

## 实测
- 双架构构建：x64（target-fix\release）+ i686（target-fix\i686-pc-windows-gnu）
  全过，无新增告警。
- 冒烟全过：[11] 标点、[12.0/12.1] 候选窗两材质、[18] 动效×缩放 9 档全通、
  [19] 拉伸圆角 mask=11、[14] 音效池化连打、[13] 皮肤热更新像素 E2E、
  [15] 横排真路径 304x252。
- 部署：deploy-37fix.ps1 → SystemIME 双通道（22:35 完，x64=15753154B、
  x86=13642240B 与构建产物一致；旧版保留 .old-223549 回滚位）。
- 引擎 workspace 测试：100 测全绿（release，0 失败——与三十六修基线持平）。

## 验证注意（给下一轮）
- **宿主必须重启**（DLL 按进程加载一次）：Excel/WPS/微信全部重开再测。
- Excel：同格连打（应即时+位置对）、换格打（~35ms 内出+位置对，不再「不在附
  近」）、公式栏输入。
- WPS 表格：同格快打（空格 90ms 节奏候选应每次可见——本修主诉）、换格打
  （首编码 ~35ms 级，不再 200ms）、WPS 文字文档（回归确认：首显不慢不跳）。
- 微信：Alt+Tab 切回后直接打字看候选位置；若仍偏，取
  `%TEMP%\hufu-tsf-trace.log` 的 qc: 行（决定下一步是否微信专属纠偏）。
- 回滚：把 `C:\Windows\SystemIME\HuFu\hufu_tsf.dll.old-223549`（及
  SysWOW64 的 hufu_tsf32.dll.old-223549）改回原名即可。

## 打包
- 本次仅 DLL 改动（engine 无行为变化），未触发版本号；下个整版打包时并入。
- 本地 commit，**不 push**（用户验证后自行决定）。
